POSTGRES_DB=smart_feeder
POSTGRES_USER=smart_user
POSTGRES_PASS=smart_pass

APP_BASE_URL=http://localhost:8080

SESSION_COOKIE_SECRET=change-me-session-secret

# 32-byte key in base64 (AES-GCM)
DEVICE_SECRET_ENCRYPTION_KEY=MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=

# Global toggle for device request signature verification.
# OFF by default: HMAC + nonce replay checks are bypassed.
DEVICE_AUTH_SIGNATURE_ENABLED=false
# Derived behavior (no separate override in code):
# DEVICE_AUTH_NONCE_ENABLED follows DEVICE_AUTH_SIGNATURE_ENABLED.
